# CELESTIA MAILBOX

## Pending Items
- installer/bootstrap governance follow-up: generated target-project `pony/` runtime should not dirty repos by default; source-layer fix must happen in bootstrap/install policy rather than by per-repo excludes alone
